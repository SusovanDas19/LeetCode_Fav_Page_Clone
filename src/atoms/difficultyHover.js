import { atom } from "recoil";

export const easyHoverAtom = atom({
    key: "easyHover",
    default: false
})
export const medHoverAtom = atom({
    key: "medHover",
    default: false
})
export const hardHoverAtom = atom({
    key: "hardHover",
    default: false
})